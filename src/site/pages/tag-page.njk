---
pagination:
  data: collections.tagList
  size: 1
  alias: tag
  filter:
    - all
    - nav
    - post
    - posts
renderData:
  title: Tagged "{{ tag }}"
  description: All posts tagged {{ tag }}
permalink: /tags/{{ tag | slug }}/
excludeFromSitemap: true
---
<h1 class="page-title">{{renderData.title}}</h1>
{% set postlist = collections[ tag ] %}
{% include "components/postlist.njk" %}

<div class="read-more">
<a href="{{ '/tags/' | url }}" class="read-more__link">See All Tags</a>
</div>
